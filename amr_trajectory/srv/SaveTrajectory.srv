# Request
string filename    # Name (and optionally path) of the file to save
float64 duration   # Time duration (in seconds) for which to filter trajectory data

---
# Response
bool success
string message

